- name: Unmount extraneous mountpoint
  ansible.builtin.command: umount --lazy "{{ this_mount }}" || true
  changed_when: true

- name: Remove extraneous mountpoint from /etc/fstab
  ansible.posix.mount:
    path: "{{ this_mount }}"
    state: absent_from_fstab
